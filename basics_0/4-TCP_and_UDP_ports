#!/usr/bin/env bash
# Script to display listening TCP and UDP ports with PID and program name

# Use ss (socket statistics) to list listening ports with program information
sudo ss -tulnpo | grep LISTEN
